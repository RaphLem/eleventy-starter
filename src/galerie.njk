---
layout: base.njk
title: Galerie
---

{%- from "macros.njk" import component -%}

<div x-data="{open: false}">
    <div class="container lg:text-color4 text-4xl" >
        {{ component("headings/title", { content: "Galerie" }) }}
    </div>
    <button @click="open = !open" class='modal-open bg-color3 border border-gray-500 text-color4 font-bold py-2 px-4 rounded-full'>
        Affiche/Cache dialogue
    </button>
    <dialog :open="open">
        Bien joué à toi !
    </dialog>
</div>